        <h2
          class="-mx-4 mb-1 flex items-center bg-white-light/30 py-3 px-2 font-extrabold uppercase dark:bg-dark dark:bg-opacity-[0.08]">
          <div class="flex items-center gap-2">
            <iconify-icon icon="solar:share-circle-bold-duotone" width="18" height="18"></iconify-icon>
            <span>Config</span>
          </div>
        </h2>
        <li class="menu nav-item -mx-4 mb-2">
          <button type="button" class="nav-link group" :class="{'active' : activeDropdown === 'admin'}"
            @click="activeDropdown === 'admin' ? activeDropdown = null : activeDropdown = 'admin'">
            <div class="flex items-center mx-2">
              <iconify-icon icon="solar:target-bold-duotone" width="18" height="18"></iconify-icon>
              <span
                class="text-black ltr:pl-3 rtl:pr-3 dark:text-[#506690] dark:group-hover:text-white-dark">Admin</span>
            </div>
            <div class="rtl:rotate-180" :class="{'!rotate-90' : activeDropdown === 'admin'}">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M9 5L15 12L9 19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"
                  stroke-linejoin="round" />
              </svg>
            </div>
          </button>
          <ul x-cloak x-show="activeDropdown === 'admin'" x-collapse class="sub-menu text-gray-500 mx-4 mb-2">
            <li x-data="{ subActive: null, hovered: false}">
              <div class="flex items-center hover:bg-gray-100 dark:text-[#888ea8] dark:hover:bg-gray-900"
                @mouseenter="hovered = true" @mouseleave="hovered = false">
                <!-- Icône dossier -->
                <iconify-icon
                  :icon="(subActive === 'error' || hovered) ? 'solar:folder-2-broken' : 'solar:folder-2-line-duotone'"
                  class="ml-2" width="16" height="16"></iconify-icon>

                <button type="button" @click="subActive === 'error' ? subActive = null : subActive = 'error'"
                  class="flex items-center w-full">
                  Entities

                  <div class="ltr:ml-auto rtl:mr-auto" style="height: 20px">
                    <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" height="16" style="margin-top: 2px"
                      x-show="hovered" @click.stop="showEditMenu = !showEditMenu, showMenu = false"></iconify-icon>
                    <iconify-icon icon="solar:add-square-linear" width="16" height="16" style="margin-top: 2px"
                      x-show="hovered" @click.stop="showMenu = !showMenu , showEditMenu = false"></iconify-icon>
                  </div>
                </button>
              </div>

              <ul class="sub-menu text-gray-500 ltr:ml-2 rtl:mr-2" x-show="subActive === 'error'" x-collapse>
                <li x-data="{ hovered: false }" @mouseenter="hovered = true" @mouseleave="hovered = false"
                  class="flex items-center w-full justify-between hover:bg-gray-100">
                  <div class="flex items-center">
                    <iconify-icon icon="solar:hamburger-menu-line-duotone" class="ml-2" width="16"
                      height="16"></iconify-icon>
                    <a href="/account/<%= account_number %>/entity/list" target="" class="ml-2">List</a>
                  </div>

                  <!-- Icône menu-dots à droite, visible uniquement au hover -->
                  <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" class="mr-1" height="16" x-show="hovered"
                    :class="{ 'rotate-90': hovered }"></iconify-icon>
                </li>
                <li x-data="{ hovered: false }" @mouseenter="hovered = true" @mouseleave="hovered = false"
                  class="flex items-center w-full justify-between hover:bg-gray-100">
                  <div class="flex items-center">
                    <iconify-icon icon="solar:hamburger-menu-line-duotone" class="ml-2" width="16"
                      height="16"></iconify-icon>
                    <a href="/account/<%= account_number %>/entity/add" target="" class="ml-2">Add</a>
                  </div>

                  <!-- Icône menu-dots à droite, visible uniquement au hover -->
                  <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" height="16" class="mr-1" x-show="hovered"
                    :class="{ 'rotate-90': hovered }"></iconify-icon>
                </li>
              </ul>
            </li>
            <li x-data="{ subActive: null, hovered: false}">
              <div class="flex items-center hover:bg-gray-100 dark:text-[#888ea8] dark:hover:bg-gray-900"
                @mouseenter="hovered = true" @mouseleave="hovered = false">
                <!-- Icône dossier -->
                <iconify-icon
                  :icon="(subActive === 'error' || hovered) ? 'solar:folder-2-broken' : 'solar:folder-2-line-duotone'"
                  class="ml-2" width="16" height="16"></iconify-icon>

                <button type="button" @click="subActive === 'error' ? subActive = null : subActive = 'error'"
                  class="flex items-center w-full">
                  Fields

                  <div class="ltr:ml-auto rtl:mr-auto" style="height: 20px">
                    <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" height="16" style="margin-top: 2px"
                      x-show="hovered" @click.stop="showEditMenu = !showEditMenu, showMenu = false"></iconify-icon>
                    <iconify-icon icon="solar:add-square-linear" width="16" height="16" style="margin-top: 2px"
                      x-show="hovered" @click.stop="showMenu = !showMenu , showEditMenu = false"></iconify-icon>
                  </div>
                </button>
              </div>

              <ul class="sub-menu text-gray-500 ltr:ml-2 rtl:mr-2" x-show="subActive === 'error'" x-collapse>
                <li x-data="{ hovered: false }" @mouseenter="hovered = true" @mouseleave="hovered = false"
                  class="flex items-center w-full justify-between hover:bg-gray-100">
                  <div class="flex items-center">
                    <iconify-icon icon="solar:hamburger-menu-line-duotone" class="ml-2" width="16"
                      height="16"></iconify-icon>
                    <a href="/account/<%= account_number %>/field-template/add" target="" class="ml-2">Custom fields</a>
                  </div>

                  <!-- Icône menu-dots à droite, visible uniquement au hover -->
                  <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" class="mr-1" height="16" x-show="hovered"
                    :class="{ 'rotate-90': hovered }"></iconify-icon>
                </li>

                <li x-data="{ hovered: false }" @mouseenter="hovered = true" @mouseleave="hovered = false"
                  class="flex items-center w-full justify-between hover:bg-gray-100">
                  <div class="flex items-center">
                    <iconify-icon icon="solar:hamburger-menu-line-duotone" class="ml-2" width="16"
                      height="16"></iconify-icon>
                    <a href="/account/<%= account_number %>/field-template/list" target="" class="ml-2">Field blocks</a>
                  </div>

                  <!-- Icône menu-dots à droite, visible uniquement au hover -->
                  <iconify-icon icon="solar:menu-dots-bold-duotone" width="16" height="16" class="mr-1" x-show="hovered"
                    :class="{ 'rotate-90': hovered }"></iconify-icon>
                </li>
              </ul>
            </li>

            <li x-data="{ hovered: false }">
              <div class="flex items-center hover:bg-gray-100 dark:text-[#888ea8] dark:hover:bg-gray-900"
                @mouseenter="hovered = true" @mouseleave="hovered = false">
                <iconify-icon icon="solar:widget-add-line-duotone" class="ml-2" width="16" height="16"></iconify-icon>
                <a href="/account/<%= account_number %>/classification/list" class="flex items-center w-full py-2 ml-2">
                  Classifications
                </a>
              </div>
            </li>
            <li x-data="{ hovered: false }">
              <div class="flex items-center hover:bg-gray-100 dark:text-[#888ea8] dark:hover:bg-gray-900"
                @mouseenter="hovered = true" @mouseleave="hovered = false">
                <iconify-icon icon="solar:widget-add-line-duotone" class="ml-2" width="16" height="16"></iconify-icon>
                <a href="/account/<%= account_number %>/mailbox/1" class="flex items-center w-full py-2 ml-2">
                  Mailbox
                </a>
              </div>
            </li>
          </ul>
        </li>
